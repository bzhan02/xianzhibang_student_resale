fix(auth): show resend-verification only when login reports unverified email

上一版把「没收到验证邮件？重新发送」常驻在登录页底部。对第一次来的
访客来说这是噪音——他还没注册过，不存在验证邮件。

改为：登录失败且错误为 email_not_confirmed 时才展开一条提示，
里面带重发入口。修改邮箱输入框会自动收起，避免换了账号后残留误导。
